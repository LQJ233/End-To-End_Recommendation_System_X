# 变更记录

> 按"代码改动必须同步文档"规范 (07_开发规范.md §9.5) 记录每次结构性修复.
> 较小的局部修改直接更新对应文档章节, 重大调整在此追加条目.

## 2026-06-08 — 架构审查 12 项修复

### 严重 / 安全

| # | 改动 | 影响文档 |
| --- | --- | --- |
| 1 | `InferenceClient` 去掉 sync wrapper. `RecommendationService` 直接调 `recommendAsync().join()`, 确保 Spring AOP 代理生效 (@CircuitBreaker/@TimeLimiter 才会真的执行). | 10 §3, 06 §2.3 |
| 2 | `UserService.adminList` 角色过滤改 SQL `EXISTS` (`BizUserMapper.xml#selectAdminPage`). 修复 total 与 records 不一致 / 跳页. | 05 §A.7, 06 §2.2 |
| 3 | `SecurityConfig.entryPoint/accessDeniedHandler` 现在返回真实 HTTP 401/403; `GlobalExceptionHandler` 全部业务异常都按错误码映射到 HTTP status. 前端 axios 拦截 401 自动 clearAuth + 跳登录页. | 05 §1.3, 06 §2.x |
| 4 | 删除注册的 select-then-insert 防重 race. `GlobalExceptionHandler.onDuplicateKey` 把约束名翻成 `username_exists / phone_exists`. | 05 §A.1, 06 §8.3 |
| 5 | `RedisCandidateRepository.replaceCandidates` 改 Lua 脚本 (DEL + RPUSH + EXPIRE 原子). 修复并发刷新时新旧候选混合 / TTL 丢失. | 04 §3.2, 06 §2.3 |
| 6 | `algorithm/training/datasets.py` 加 `_enforce_time_split`, 按 `training.enforceTimeSplit` 配置过滤 `feature_time >= label_time` 的样本并打印计数. | 06 §3.2, 09 §4 |

### 算法 / 质量

| # | 改动 | 影响文档 |
| --- | --- | --- |
| 7 | `TwoTowerModel` 引入共享 `item_id_embed`, 给 user tower 序列查表 + item tower item_id 共用同一张 embedding. 召回质量提升 (原本各自一张表). | 06 §4.2 |
| 8 | `FeatureConfig.DEFAULT_CAT_VOCAB_SIZE` 把 `item_id` 提到 2M, `user_id` 提到 1M, 修复哈希冲突过高的问题. | 06 §4.1, 09 §6 |
| 9 | `AuthRedisRepository` 加 `trackUserToken / invalidateAllForUser`. 管理员禁用 / 改密 / 调角色时调 `AuthService.forceLogoutAll`, 真正下线已签发 token. | 05 §A.8/A.9/A.10, 10 §5 |
| 10 | `redis_client.read_user_bundle` 把 6 次 Redis 调用合并成一次 pipeline. `service.py` 用 bundle, 并把 geo/tag_pref 透传给 `lbs_recall/tag_recall` 避免重复读. | 10 §4, 06 §5.2 |

### 工程默认值

| # | 改动 | 影响文档 |
| --- | --- | --- |
| 11 | `SecurityConfig.anyRequest().denyAll()`, `/api/v1/orders/**` 显式 `authenticated()`. 新接口忘记登记会立即 403, 不会裸奔. | 10 §3, 07 §4.x |
| 12 | `JwtTokenProvider` 启动校验 jwtSecret. 默认值 "change_me" 在非 local profile 下直接拒绝启动; local 下大声 warning. | 10 §5, 07 §9.x |

### 配套测试 / 部署

- Tianchi 数据集 importer / Spark sample 上限 1000 万行未变, 见 09.

## 2026-06-08 — 工程基线 6 项

| # | 改动 | 文件 / 文档 |
| --- | --- | --- |
| A | 单元 + 切片测试: `RequestIdGeneratorTest` / `UserIdGeneratorTest` / `JwtTokenProviderTest` / `LoginAttemptServiceTest` / `AuthControllerTest` (MockMvc + GlobalExceptionHandler) + Python pytest `tests/test_recall_runner.py` / `test_features.py` | `backend/src/test/**`、`algorithm/tests/**` |
| B | Flyway 接管 schema. `V1__init_schema.sql` + `V2__seed_roles_items.sql` + `V3__refresh_token_audit.sql`; `init_db.ps1` 只建 database, 启动时 Flyway 迁移. `spring.flyway.baseline-on-migrate=true` 兼容已有库. | `backend/.../db/migration/*`、`application.yml`、`deploy/init_db.ps1` |
| C | refresh token 流程: `RefreshTokenRepository` (Redis 一次性 + 用户级 Set); `AuthService.refresh()` 轮转; `LoginResponse` 新增 `refreshToken/refreshExpiresIn`; 前端 `tryRefresh()` + axios 401 拦截自动重放. `forceLogoutAll` 同时 revoke refresh. | 后端 auth/redis/dto/controller、`frontend/src/api/http.js`、`frontend/src/stores/user.js`、`05_API §A.5.5`、`04 §6.5` |
| D | 分布式 trace: Micrometer Tracing + OTEL bridge (Java) + `TraceHeaderInterceptor` 把 W3C `traceparent` 注入推理 RPC; Python `TraceContextMiddleware` 解析 traceparent 写入 logging context; `RecommendationService.refresh` 加 `@Observed`. 响应带 `x-trace-id`. | `backend/.../common/config/TraceHeaderInterceptor.java`、`HttpClientConfig`、`algorithm/inference/tracing.py`、`05_API §A.14` |
| E | Python 推理 Prometheus: `prometheus-fastapi-instrumentator` 暴露 `/metrics`; 自定义 `recsys_recall_channel_size/latency`、`recsys_rank_latency_ms`、`recsys_fallback_total`. Java 端 `/actuator/prometheus` 已开. | `algorithm/inference/metrics.py`、`app.py`、`service.py`、`05_API §A.13` |
| F | CORS 收紧: `app.cors.allowedOrigins` 配置化, 默认只放 `http://localhost:5173`. `WebConfig` 读配置, 不再 `allowedOriginPatterns("*")`. 暴露 `x-trace-id` header. | `AppProperties.Cors`、`WebConfig`、`application.yml`、`conf/application-local.yml`、`05_API §1.x` |
